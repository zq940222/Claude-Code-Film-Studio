# film-studio 影视工作台

Claude Code 插件 film-studio 的领域词汇表：AI 影视创作（短剧/电影短片/动漫番剧）从建项到发布的全流程概念。

## Language

**工作区（工作目录）**:
由 `/new-drama` 生成的创作根目录，含 `projects/`、`tools/` 和工作区 CLAUDE.md；一个工作区可容纳多个项目。仪表盘等外部观测工具以它为观测单元。
_Avoid_: 工程目录、repo

**项目**:
一部剧的全部产物与档案，落在 `projects/<片名>/` 下，档案为 `project.json`（形态、画风、各阶段状态、积分记录）。
_Avoid_: 剧目、作品集

**创作形态（medium）**:
项目三选一的类型——短剧 / 电影短片 / 动漫番剧，驱动各 agent 的创作法则切换。
_Avoid_: 题材、类型（指剧情 genre 时另说）

**门禁**:
流水线上必须由用户在对话中明确确认才能通过的关卡，共四道：剧本定稿、设定图定稿、积分报价确认、发布确认。任何外部工具（含仪表盘）都不承载门禁确认。
_Avoid_: 审批、checkpoint

**镜头**:
生成与质检的最小单元，命名 `ep{NN}/sh{NN}`，产物为 `sh{NN}.mp4`。
_Avoid_: 片段、clip

**shotlist**:
摄影指导与视频生成师之间的交接件兼生成日志（`shotlist.json`），记录每镜提示词、模式、submit_id、状态与产物路径；生成过程中实时更新。
_Avoid_: 任务清单、队列

**风格锁（STYLE LOCK）**:
`style-bible.md` 中的英文关键词块，全剧画风的单一真源；出图、视频提示词逐字前置，精剪调色按其意图选滤镜。
_Avoid_: 风格模板、preset（preset 指画风预设条目本身）

**仪表盘**:
独立于插件的本地 web 应用（film-studio-dashboard），对选定工作区做只读观测 + 轻操作；不触发生成、不确认门禁、不回写工作区文件。
_Avoid_: 控制台、管理后台

**轻操作**:
仪表盘允许的非破坏动作边界：复制建议命令、刷新积分（`dreamina user_credit` 只读查询）。超出此边界即属控制行为，仪表盘不做。
_Avoid_: 写操作
